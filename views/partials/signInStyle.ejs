<style>
  
  body {
    margin: 0;
    padding: 0;
    overflow: hidden;
  }  
  
  .background-photo {
    position: fixed;
    top: 0;
    left: 0;
    width: 100vw;
    height: 100vw;
    background-image: url('images/balloons2.jpg');
    background-size: cover;
    filter: blur(5px);
    z-index: -1;
  }

  .container {
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    height: 100vh;
    z-index: 1;
    text-align: center;
  }

  .form-container {
    background-color: transparent;
    text-align: center;
    max-width: 400px;
  }

  .form-container input[type="text"],
  .form-container input[type="password"] {
    background-color: rgba(255, 255, 255, 0.8);
    border-radius: 10px;
  }
</style>