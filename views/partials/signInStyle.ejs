<style>
  /* This file does not have errors, vscode just doesn't like this ejs */
  body {
    margin: 0;
    padding: 0;
    overflow: hidden;
  }  
  
  .background-photo {
    position: fixed;
    top: 0;
    left: 0;
    width: 100vw;
    height: 100vw;
    background-image: url(<%= imagePath %>);
    background-size: cover;
    z-index: -1;
  }

  .container {
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    height: 100vh;
    z-index: 1;
    text-align: center;
    max-width: 400px;
    background-color: transparent;
  }

  .form-container {
    background-color: transparent;
    text-align: center;
    max-width: 400px;
  }

  input {
    background-color: rgba(255, 255, 255, 0.8);
    border-radius: 10px;
  }
</style>