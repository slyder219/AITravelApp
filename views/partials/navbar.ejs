<nav class="navbar has-shadow is-info" role="navigation" aria-label="main navigation">
        <div class="navbar-brand">
            <% if (loggedInBool) { %>
          <a class="navbar-item" href="/welcome">
            <% } else { %>
            <a class="navbar-item" href="/">
                <% } %>
            <h1 class="title py-3">Odyssey</h1>
          </a>
        </div>
      
        <div class="navbar-menu">
          <div class="navbar-start">
            <% if (loggedInBool) { %>
            <a class="navbar-item" href="/logout">Logout</a>
            <% } else { %>
            <a class="navbar-item" href='/loginPage'>Login</a>
            <a class="navbar-item" href="/register">Register</a>
            <% } %>
            <a class="navbar-item" href="/search">Search</a>
            <a class="navbar-item" href="/databaseTest">To Database</a>
          </div>
          <div class="navbar-end">
            <% if (loggedInBool) { %>
            <span class="navbar-item">Welcome, <%= username %> </span>
            <% } else { %>
            <span class="navbar-item">Not logged in</span>
            <% } %> 
          </div>
        </div>
      </nav>